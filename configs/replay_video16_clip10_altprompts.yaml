run:
  id: null
  scenario: DBE
  name: control_replay_video16_clip10_altprompts
  description: "Replay del clip video16_clip10 con set de prompts alternativo ppe_alt_v1 (worker/hardhat/reflective_vest), GDINO-tiny. Test de agnosticismo del GT, 2026-07-18."

input:
  type: media_jsonl
  path: /home/simonll4/projects/e-ovrt_media-plane/runs/run_20260718_165824_dbe_grounding_dino_ef4cd4/detections.jsonl

patterns:
  file: patterns/cr01_cr02_v2.yaml
  active_ids:
    - CR-01
    - CR-02

outputs:
  base_dir: ../runs
  save_pattern_events_jsonl: true
  save_alerts_jsonl: true
  save_metrics_jsonl: true
  save_errors_jsonl: true
  save_summary_json: true

logging:
  level: INFO

run:
  id: null
  scenario: DBE
  name: replay_hf_field
  description: "Replay de detecciones de labs con el perfil de campo (persistencia + expiracion + cooldown + pose adaptativa + memoria de cobertura)."

input:
  type: media_jsonl
  path: ../fixtures/hf_media/latest/detections.jsonl

patterns:
  file: patterns/cr01_cr02_field_v1.yaml
  active_ids:
    - CR-01
    - CR-02

outputs:
  base_dir: ../runs
  save_pattern_events_jsonl: true
  save_alerts_jsonl: true
  save_metrics_jsonl: true
  save_errors_jsonl: true
  save_summary_json: true

logging:
  level: INFO
